/*
package test;
import actor.Actor;
/**
 * 
 * @author Amanda
 * Create pairs of actor objects and in a loop engage them in combat.
 * Assess the suitability of the calculations which use health and strength.
 *
 */
/*
public class TestActorSkirmishes {	
			
	
	public static void main (String[]args){
		
		Actor attacker = new Actor();		//Creates two actor objects
		Actor defender = new Actor();
		
		System.out.println(attacker.toString());		//Prints out their characteristics
		System.out.println(defender.toString());
		
		do {
		attacker.combatRound(defender);				//Runs combatRound and prints actor's characteristics between each round.
		System.out.println("\n"+attacker.toString());
		System.out.println(defender.toString());
		}while(attacker.getHealth()>0 && defender.getHealth()>0);	//Loops until one of the actors are at 0 health.
		

	}
}
*/
